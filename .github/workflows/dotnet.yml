# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

permissions:
  contents: write
  deployments: write
  packages: write
  pages: write

on:
  pull_request:
    types: [ closed ]
    branches: [ main ]

jobs:
  publish:
    defaults:
      run: 
        working-directory: Source/
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 7.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore -f net7.0 -c release
    - name: Authenticate to GitHub NuGET Registry
      run: dotnet nuget add source --username OliverKarger --password ${{ secrets.GITHUB_TOKEN }} --store-password-in-clear-text --name github "https://nuget.pkg.github.com/loco-software/index.json"
    - name: Publish Package
      run: dotnet nuget push bin/release/LocoSoftware.ImGuiWrapper.1.0.0.nupkg --source "github"

          
